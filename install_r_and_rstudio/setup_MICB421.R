course <- "MICB421"
# course specific packages
packages_course <- "pwr"
source(file.path(root, "setup", "setup.R"))
# The following variables are loaded from setup file:
# images_path, point to images with `r file.path(images_path, <filename>)`
# css_path, point to css file with `r file.path(css_path, <filename>)`
# academic_year
# course
# r_version
# r_legacy_version
# contact
# contact_short
# office_hours